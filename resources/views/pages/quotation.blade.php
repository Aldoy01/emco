@extends('layouts.public') @section('title','Hubungi Sales EMKO') @section('content')
<section class="page-title"><p class="eyebrow">Hubungi Sales</p><h1>Hubungi Sales EMKO</h1><p>Isi kebutuhan proyek agar sales dapat menghitung harga final berdasarkan qty, lokasi, shipping, dan kebutuhan teknis.</p></section>
<section class="section narrow">@if(session('success'))<div class="alert success">{{ session('success') }}</div>@endif<form class="rfq-form" method="post" action="{{ route('quotation.store') }}">@csrf<input type="text" name="website" class="honeypot" tabindex="-1" autocomplete="off"><div class="form-grid"><label>Nama<input name="name" value="{{ old('name') }}" required></label><label>Nama Perusahaan<input name="company" value="{{ old('company') }}" required></label><label>Email<input type="email" name="email" value="{{ old('email') }}" required></label><label>WhatsApp / Telepon<input name="phone" value="{{ old('phone') }}" required></label><label>Produk<select name="product_id" required><option value="">Pilih produk</option>@foreach($products as $product)<option value="{{ $product->id }}" @selected(old('product_id',$selectedProduct)==$product->id)>{{ $product->product_name }}</option>@endforeach</select></label><label>Quantity<input type="number" min="1" name="quantity" value="{{ old('quantity',1) }}" required></label><label>Lokasi Proyek<input name="project_location" value="{{ old('project_location') }}" required></label><label>Deadline Proyek<input type="date" name="project_deadline" value="{{ old('project_deadline') }}"></label></div><label>Kebutuhan Teknis<textarea name="technical_needs" rows="5" placeholder="Contoh: 2 unit genset, butuh AMF/ATS/synchro/load sharing, kapasitas genset, panel existing">{{ old('technical_needs') }}</textarea></label>@if($errors->any())<div class="alert error">Mohon periksa kembali data yang wajib diisi.</div>@endif<button class="btn btn-gold" type="submit">Kirim ke Sales</button></form></section>
@endsection
