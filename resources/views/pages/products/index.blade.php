@extends('layouts.public') @section('title',($categoryPage->name ?? 'Katalog Produk').' - EMKO') @section('content')
<section class="page-title"><p class="eyebrow">Product Catalogue</p><h1>{{ $categoryPage->name ?? 'Katalog Produk EMKO' }}</h1><p>{{ $categoryPage->description ?? 'Cari berdasarkan product code, kategori, fungsi controller, dan harga estimasi Rupiah setelah diskon.' }}</p></section>
<section class="section"><form class="filter-bar" method="get" action="{{ route('products.index') }}"><input name="q" value="{{ request('q') }}" placeholder="Cari product code atau nama produk"><select name="category"><option value="">Semua kategori</option>@foreach($categories as $category)<option value="{{ $category->slug }}" @selected($selectedCategory === $category->slug)>{{ $category->name }}</option>@endforeach</select><button class="btn btn-gold">Filter</button></form><div class="product-grid">@forelse($products as $product) @include('components.product-card',['product'=>$product]) @empty <p>Produk tidak ditemukan.</p> @endforelse</div><div class="pagination-wrap">{{ $products->links() }}</div></section>
@endsection