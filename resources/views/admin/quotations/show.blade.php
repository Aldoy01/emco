@extends('layouts.admin') @section('content')@php($hideCommercial = config('emko.hide_commercial_values'))<h1>Detail Lead</h1><section class="admin-panel split top-align"><div><h2>{{ $quotation->lead->company }}</h2><p>{{ $quotation->lead->name }}<br>{{ $quotation->lead->email }}<br>{{ $quotation->lead->phone }}</p><p>Produk: <strong>{{ $quotation->product->product_name }}</strong><br>Qty: {{ $hideCommercial ? '' : $quotation->quantity }}<br>Lokasi: {{ $quotation->project_location }}<br>Deadline: {{ optional($quotation->project_deadline)->format('d M Y') ?: '-' }}</p><p>{{ $quotation->technical_needs }}</p></div><form class="rfq-form" method="post" action="{{ route('admin.quotations.update',$quotation) }}">@csrf @method('PUT')<label>Status<select name="status">@foreach($statuses as $key=>$label)<option value="{{ $key }}" @selected($quotation->status===$key)>{{ $label }}</option>@endforeach</select></label><label>Catatan Follow-up<textarea name="follow_up_notes" rows="8">{{ $quotation->follow_up_notes }}</textarea></label><button class="btn btn-gold">Update Lead</button></form></section>@endsection
